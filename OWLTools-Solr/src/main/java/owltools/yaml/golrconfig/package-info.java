/**
 * <h2>YAML GOlr Configuration</h2>
 * 
 * <p>
 * This package supplies the tools necessary to parse, probe, and dump the YAML file configurations for BBOP-JS constrained Solr indexes.
 * Specifically, it can parse YAML files for use with the {@link owltools.solrj.FlexSolrDocumentLoader}
 * and take multiple YAML files and turn them into a Solr schema that is compatible {@link owltools.yaml.golrconfig.SolrSchemaXMLWriter}.
 * </p>
 */
package owltools.yaml.golrconfig;

